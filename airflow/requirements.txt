# CatalogGuard runtime dependencies remain isolated from application requirements.
# Airflow itself is installed separately with its official constraints.
# Required by AIRFLOW__DATABASE__SQL_ALCHEMY_CONN's postgresql+psycopg dialect.
psycopg[binary]
# Used by the existing core.upload_validator and etl.db_loader implementation.
pandas==3.0.3
