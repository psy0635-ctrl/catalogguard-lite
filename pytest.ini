[pytest]
addopts = -m "not e2e and not performance"
markers =
    e2e: requires the CI PostgreSQL, Redis, FastAPI, and Celery services
    performance: opt-in PostgreSQL query-plan benchmark with generated data
